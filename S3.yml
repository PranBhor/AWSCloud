#creation of simple s3 bucket
---
  - name: creation of s3 bucket
    hosts: localhost
    tasks:
    - name: aws S3
      s3_bucket:
        name: mybcktrial
        state: present
        region: ap-south-1
        versioning: no
      
